#include <stdio.h>
int factorial(int mul)
{       int i;
        mul = mul * i;
        return 0;
} 
    int main()
    {   
        int n;
        printf("enter no:\n");
        scanf("%d",&n);
        factorial(n);
        return 0;
    }
