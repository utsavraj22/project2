#include <stdio.h>
int main(){
    int num;
    printf("Enter the number :\n");
    scanf("%d",&num);
        if(num%2==0&&num==0)
        printf("Number is even.");
        else 
        printf("Number is odd.");
    return 0;
}